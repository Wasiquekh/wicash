import React from "react";

const Header = () => {
  return (
    <div>
      {/* Brand / Header */}
      <header className="w-full py-4 px-8 border-b border-gray-300 bg-white flex items-center">
        <h1 className="text-2xl font-bold">Wi Cash</h1>
      </header>
    </div>
  );
};

export default Header;
